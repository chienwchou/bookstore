export interface BookData {
    _id: string;
    title: string;
    price: number;
    category: string;
    description: string;
}

export interface BookFormData {
    title: string;
    price: number;
    category: string;
    description: string;
}